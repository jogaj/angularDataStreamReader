import { PostOwnerPipePipe } from '../post-owner-pipe.pipe';

describe('PostOwnerPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PostOwnerPipePipe();
    expect(pipe).toBeTruthy();
  });
});
